# Token list supported by interpreter

class TokenType(object):
    ADD, SUBTRACT, MULTIPLY, DIVIDE = 'ADD', 'SUBTRACT', 'MULTIPLY', 'DIVIDE'
    L_PAREN, R_PAREN = 'L_PAREN', 'R_PAREN'
    INTEGER = INTEGER
